# KGM 성수서비스센터 — Codex 작업 기록

> 이 문서는 Codex가 앞으로 `https://kgm-seongsu.co.kr/un/`을 안전하게 유지·수정하기 위한 기준 문서다.
> **가장 중요한 원칙은 어떤 기능을 추가하더라도 현재 사용 중인 내부 페이지의 기존 기능을 깨뜨리지 않는 것이다.**

## 사용자 및 전용 기능

- 담당자: 윤유현 부장
- 주 담당자 이메일: `un910315@gmail.com`
- 내부 업무 페이지: `https://kgm-seongsu.co.kr/un/`
- Firebase 프로젝트: `unmotors`
- 배포 저장소: `https://github.com/un910315-cyber/kgm-seongsu-home`
- 배포 브랜치: `master`

## 실제 운영 원본 — 매우 중요

- 실제 배포 저장소 로컬 경로: `C:\tmp\kgm-seongsu-home-repo`
- 운영 페이지 파일: `un/index.html`
- 주요 기능 코드: `un/app-module.js`, `un/app-pages.js`, `un/app-features.js`, `un/app-insurance.js`
- 스타일: `un/styles.css`
- 서비스워커/캐시: `un/sw.js`
- 바탕화면 `KGM견적/dist/index.html`은 예전 단일 HTML 구조이므로 **운영 배포 원본으로 사용하지 않는다.**
- 작업 시작 전 반드시 `git pull --ff-only origin master`로 최신 운영본을 확인한다.

## 절대 안전 원칙

1. 작업 전 저장소가 깨끗한지 `git status`로 확인한다.
2. 항상 현재 정상 커밋 해시를 기록한다.
3. 기능 변경 전 `rollback-before-작업명-날짜` 형태의 Git 태그를 만든다.
4. 변경 파일을 별도 커밋으로 남긴다. 강제 푸시와 amend는 하지 않는다.
5. 기존 운영 파일 전체를 과거 로컬 파일로 덮어쓰지 않는다.
6. 필요한 부분만 최소 변경하며 기존 기능과 무관한 코드는 건드리지 않는다.
7. 배포 전 JavaScript 문법 검사와 `git diff --check`를 반드시 실행한다.
8. 배포 후 실제 URL에서 새 코드와 캐시 버전을 직접 확인한다.
9. 문제가 확인되면 즉시 직전 롤백 태그 또는 정상 커밋으로 되돌린다.
10. 사용자 데이터 삭제·경로 변경·Firebase 규칙 변경은 특히 보수적으로 처리한다.

## 표준 배포 절차

1. 최신 `master` 가져오기
2. 현재 정상 커밋 기록
3. 원격 롤백 태그 생성
4. 최소 범위로 수정
5. 관련 모든 스크립트 문법 검사
6. `git diff`와 `git diff --check` 검토
7. 서비스워커 `CACHE_VERSION`과 HTML의 `?v=` 버전을 함께 갱신
8. 새 커밋 생성
9. `master`와 롤백 태그를 GitHub에 push
10. GitHub Pages 반영 대기
11. 라이브 `index.html`, 관련 JS, `sw.js`의 버전과 변경 코드 확인

## Firebase 주의사항

- Realtime Database에 새로운 최상위 경로를 추가하면 Firebase 규칙에도 반드시 같은 경로를 추가한다.
- 기존 `$other` 규칙은 차단 상태이므로 규칙 추가 없이 새 경로를 사용하면 `PERMISSION_DENIED`가 발생한다.
- 개인 전용 데이터는 화면에서 메뉴만 숨기지 말고 Firebase 규칙에서도 이메일을 제한한다.
- 개인 전용 이메일 조건:
  - `auth != null && auth.token.email == 'un910315@gmail.com'`
- 기존 규칙 전체를 확인하지 않은 상태에서 규칙 전체를 임의로 덮어쓰지 않는다.

## 2026-07-27 작업 기록 — 예약 및 체크 현황판

### 요청

- 차량 예약과 개인 체크 업무를 한 화면에서 관리
- 등록, 수정, 삭제, 완료 및 다시 열기
- 입력한 내용을 한눈에 볼 수 있는 카드형 현황판
- 검색과 구분/상태 필터
- 진행 중 예약, 체크 업무, 오늘 일정, 완료 건수 요약
- 윤유현 부장 본인만 열람·사용

### 구현

- 메뉴: `예약 및 체크 현황판`
- Firebase 경로: `reservationChecks`
- 화면 접근 이메일 제한: `un910315@gmail.com`
- Firebase 읽기/쓰기 규칙도 같은 이메일로 제한
- 필드: 구분, 상태, 내용, 예정일, 시간, 차량번호, 고객명, 연락처, 상세 메모
- 지난 미완료 일정은 `기한 지남`으로 강조
- 전화번호 클릭 시 전화 연결
- 다른 계정은 메뉴 비표시, 직접 페이지 호출 차단, Firebase 구독 미실행

### 배포 기록

- 기능 배포 커밋: `086a80d` — 개인 전용 예약 및 체크 현황판 추가
- 배포 전 정상 커밋: `b4ddfa1`
- 원격 롤백 태그: `rollback-before-reservation-20260727`
- 캐시 버전: `v120-2026-07-27`
- 라이브 반영 확인 완료

## 향후 작업 시 체크리스트

- [ ] 사용자 요청과 실제 운영 최신본이 일치하는지 확인
- [ ] 작업 전 정상 커밋 및 롤백 태그 확보
- [ ] 기존 로그인·입출고·보험·연차·게시판·매출·견적 기능 영향 검토
- [ ] Firebase 신규 경로/권한 영향 검토
- [ ] 문법 검사 및 diff 검토
- [ ] 캐시 버전 동시 갱신
- [ ] 별도 커밋과 원격 롤백 태그 push
- [ ] 실제 사이트 반영 확인
- [ ] 사용자에게 배포 커밋과 롤백 지점 보고
## 문서 우선순위와 Claude 기록 인수 원칙

- 앞으로 이 프로젝트의 **최우선 작업 문서는 `CODEX.md`**다.
- `CLAUDE.md`와 `claude-handoff-brief.md`는 과거 의사결정과 업무 맥락을 확인하는 참고 자료다.
- Claude 문서와 실제 운영 코드가 다르면 다음 순서로 판단한다.
  1. GitHub `origin/master`의 최신 운영 코드
  2. 라이브 사이트에서 확인된 상태
  3. `CODEX.md`의 최신 작업 기록
  4. `CLAUDE.md` 및 과거 인수인계 문서
- 과거 Claude 문서의 “`un/index.html` 단일 파일” 및 “바탕화면 `dist/index.html`이 최신” 설명은 현재 기준으로 오래된 정보다.
- 현재 내부 페이지는 `index.html`과 `app-module.js`, `app-pages.js`, `app-features.js`, `app-insurance.js`, `styles.css`로 분리되어 있다.
- Claude 문서를 삭제하지는 않는다. 과거 결정의 근거를 추적할 수 있도록 보존한다.

## 사용자 업무 맥락과 협업 방식

- 사용자: 윤유현 부장
- 소속: 케이지모빌리티성수서비스센터(주) · UN Motors
- 주 사용 언어와 커밋 메시지: 한국어
- 홈페이지, 내부 시스템, 디지털 명함, 인사·노무 실무까지 폭넓게 직접 관리한다.
- 작업 방식은 작은 개선을 빠르게 반복하는 형태를 선호한다.
- 디자인은 화려한 강조보다 절제되고 업무 가독성이 높은 구성을 선호한다.
- 법률·개인정보·보안 리스크는 임의로 단정하지 않고 공식 자료 또는 전문가 확인을 권한다.
- 조직 위계와 직함·표시 순서를 정확하게 유지한다.

## 현재 내부 페이지 구조

### 주요 운영 메뉴

- 대시보드
- 입출고 목록
  - 입출고 목록
  - 현황 목록
  - 금일 출고 예정
  - 출고완료
- 보험 미결
- 연차관리
- 공지·게시판
- 매출 보고
- 견적 도우미
- 보험사 연락처
- 거래처 연락처
- 예약 및 체크 현황판 — `admin` 역할 전용
- 사용자 관리
- 블랙 관리는 출고완료 화면의 관리자 전용 진입 버튼으로 접근

### 현재 역할 메뉴 — 코드 확인 기준

- `admin`: dashboard, list, status, complete, out, migyeol, leave, board, estimate, insurance, vendors, sales, reservation, usermgmt
- `staff`: status, complete, leave, board, estimate, insurance, vendors
- `viewer`: status, complete, leave, board, vendors
- `reservation`은 `ROLE_MENUS`, 페이지 진입 가드, Firebase 관리자 규칙으로 보호한다.
- 메뉴 숨김만 보안으로 간주하지 않는다. 민감 기능은 페이지 진입 가드와 Firebase 규칙을 함께 적용한다.

## Firebase 데이터 경로 — 2026-07-27 규칙 기준

- `allowedEmails`
- `records`
- `blacklist`
- `leaveEmployees`
- `leaveUsage`
- `leaveRequests`
- `annualLeaveNotices`
- `notices`
- `board`
- `companyEvents`
- `kgmDailyCount`
- `salesDaily`
- `monthlyDeposit`
- `monthlyTarget`
- `workCodes`
- `aosClaims`
- `insuranceContacts`
- `vendorContacts`
- `reservationChecks` — `allowedEmails` 값이 `admin`인 사용자 전용
- `$other`는 읽기·쓰기 모두 차단

Firebase 규칙 주소:
`https://console.firebase.google.com/project/unmotors/database/unmotors-default-rtdb/rules`

## 연차 및 조직 업무 맥락

### 연차 결재 흐름

신청 → 부서장 → 시스템 관리자 → 대표 → 최종 승인 시 `leaveUsage` 자동 등록

자동 처리 원칙:

- 대표 신청은 즉시 최종 승인
- 임원 신청은 부서장·관리자 단계를 건너뛰고 대표 결재
- 부서장은 부서장 단계를 자동 통과
- 팀에 부서장이 없으면 부서장 단계를 자동 통과
- 신청자가 시스템 관리자이고 부서장 단계도 자동 통과된 경우 관리자 단계도 자동 통과

### 조직 표시 주의

- 대표: 양은주
- 임원 및 대표 직속 인력의 결재선을 함부로 변경하지 않는다.
- `team`은 결재 라우팅용이다.
- `displayTeam`은 조직도 표시용이다.
- 사고전담부처럼 결재 부서와 조직도 표시 부서가 다른 경우 두 필드를 구분한다.

### 연차 사용촉진

- 통지서 기능은 법적 증빙과 관련되어 있으므로 구조·일자·승인 기록 변경 시 특히 신중해야 한다.
- 회계연도 기준 1차 촉진은 일반적으로 7월 초, 2차 지정은 10월 말 시점과 연관된다.
- 법적 효력에 관한 답변이나 기능 변경은 최신 법령과 전문가 검토가 필요하다.

## 공개 페이지와 디지털 명함

- `/` — 메인 홈페이지
- `/digital-card/` — 박경영 총괄이사
- `/digital-card-lee/` — 이동근 정비팀장
- `/digital-card-yang/` — 양은주 대표이사
- `/digital-card-yoon/` — 윤유현 부장
- `/kakao-qr/` — 카카오톡 QR
- 명함 수정 시 직함, 전화번호, `og:image`, `og:url`, Twitter 메타태그를 함께 확인한다.
- 카카오톡 미리보기 캐시가 갱신되지 않으면 OG 이미지 파일명을 새 버전으로 변경하고 HTML 경로도 함께 수정한다.
- `CNAME`, GitHub Pages 설정, 가비아 DNS는 명시적인 요청과 충분한 확인 없이 변경하지 않는다.

## 코드 안정성 세부 규칙

- `onValue` 콜백의 추가 렌더는 `try-catch`로 감싸 한 기능의 오류가 전체 화면을 멈추지 않도록 한다.
- 일반 스크립트에서 ES module의 함수를 호출해야 하면 `window._기능명` 형태로 명시적으로 내보낸다.
- `statusBadge`처럼 흔한 함수명은 충돌 위험이 있으므로 도메인 접두사를 사용한다.
- 인라인 이벤트 속성 안에서 복잡한 한글 문자열 비교가 필요하면 기존 코드의 유니코드 이스케이프 관습을 따른다.
- 역할 라벨이나 옵션을 바꿀 때 `ROLE_MENUS`, 역할 라벨/설명/색상, 사용자 관리 드롭다운 등 중복 정의를 모두 검색한다.
- 명함 공통 구조 변경은 4개 명함을 함께 비교한다.
- 서비스워커 변경 시 운영 `/un/` 캐시와 시험용 `/un-v2/` 캐시를 혼동하지 않는다.
- 배포 후 브라우저에 이전 버전이 남으면 새 `CACHE_VERSION` 확인, 앱 완전 종료 후 재실행, `Ctrl+F5` 순서로 점검한다.

## Codex 기록 유지 규칙

- 기능을 추가·수정·삭제할 때마다 이 문서의 작업 기록에 날짜, 목적, 변경 파일, Firebase 영향, 커밋, 롤백 태그, 라이브 확인 결과를 추가한다.
- 운영 구조가 바뀌면 과거 설명을 조용히 방치하지 말고 현재 기준과 과거 기준을 명확히 구분한다.
- 작업이 끝나도 로컬에만 기록하지 않고 GitHub 저장소의 `CODEX.md`를 새 커밋으로 보존한다.
- 문서 변경 자체도 Git 기록으로 복구 가능하게 만든다.
## 2026-07-27 작업 기록 — 예약 현황판 메뉴 위치 변경

- 요청: 사이드바 하단의 `예약 및 체크 현황판`을 `입출고 목록` 하위로 이동
- 위치: `출고완료` 바로 아래
- 개인 이메일 전용 노출 및 Firebase 권한은 그대로 유지
- 예약 페이지를 열었을 때 입출고 하위 메뉴가 펼쳐진 상태를 유지하도록 `app-pages.js` 보완
- 변경 파일: `un/index.html`, `un/app-pages.js`, `un/sw.js`
- 캐시 버전: `v121-2026-07-27`
- 배포 커밋: `09a509a` — 예약 현황판을 입출고 하위 메뉴로 이동
- 롤백 태그: `rollback-before-reservation-menu-move-20260727`
- 라이브 메뉴 순서와 캐시 버전 반영 확인 완료
## 2026-07-27 작업 기록 — 예약 현황판을 전체 관리자에게 개방

- 요청: 윤유현 개인 전용에서 Firestore 역할이 `admin`인 모든 관리자 사용으로 변경
- 화면: `ROLE_MENUS`의 admin 사용자에게 메뉴 표시
- 페이지: `window._userRole === 'admin'` 진입 가드 적용
- 데이터 구독과 등록·수정·완료·삭제: admin 역할 가드 적용
- Firebase: `allowedEmails/{emailKey}` 값이 `admin`인 계정만 `reservationChecks` 읽기·쓰기 허용
- Firebase 안전 강화: `allowedEmails` 쓰기도 기존 admin만 가능하도록 변경
- 누락돼 있던 `autodent7@gmail.com` 관리자 미러를 동기화
- 규칙 백업: `firebase-rules-backups/realtime-before-admin-reservation-20260727.json`
- 변경 파일: `un/app-module.js`, `un/app-pages.js`, `un/index.html`, `un/sw.js`, `CODEX.md`
- 캐시 버전: `v122-2026-07-27`
- 배포 커밋: `9105ccc` — 예약 현황판을 관리자 전체에 개방
- 롤백 태그: `rollback-before-admin-reservation-access-20260727`
- Firebase 규칙과 라이브 캐시·관리자 가드 반영 확인 완료
## 2026-07-28 작업 기록 — 대시보드 매출 위젯 제거

- 요청: 매번 갱신하기 번거로운 대시보드 하단 매출 표시 제거
- 제거 화면: `매출 일별 추이`, `매출 보고`, `누적 매출`
- 유지 화면: 대시보드 상단 사고차량 정보, 사고차 통계, 입금현황, 정비차량(KGM) 현황
- `매출 보고` 메뉴와 입력 기능, Firebase 매출 데이터는 삭제하지 않고 유지
- 정비차량(KGM) 현황은 대시보드 하단에서 전체 너비로 표시
- 변경 파일: `un/index.html`, `un/sw.js`, `CODEX.md`
- 캐시 버전: `v123-2026-07-28`
- 배포 커밋: `c4e5100` — 대시보드 하단 매출 위젯 제거
- 롤백 태그: `rollback-before-dashboard-sales-removal-20260728`
- 라이브에서 매출 위젯 제거와 KGM·매출 보고·예약 기능 유지 확인 완료

## 2026-07-28 작업 기록 — 대시보드 정비차량 현황 제거

- 요청: 대시보드 제일 하단의 `정비차량 (KGM) 현황` 제거
- 화면 카드만 제거하고 `매출 보고` 메뉴·입력 기능과 Firebase 기존 데이터는 유지
- 대시보드 상단 사고차량 정보, 사고차 통계, 입금현황은 유지
- 변경 파일: `un/index.html`, `un/sw.js`, `CODEX.md`
- 캐시 버전: `v124-2026-07-28`
- 롤백 태그: `rollback-before-dashboard-kgm-removal-20260728`
- 배포 커밋: `e4f0579` — 대시보드 정비차량 현황 제거`r`n- 라이브에서 캐시 `v124`·정비차량 카드 제거·현황목록·매출 보고·예약 기능 유지 확인 완료
## 2026-07-30 작업 기록 — 긴급 정비 접수 페이지

- 배경: BOLT 전산 장애로 당일 정비 접수가 어려운 상황에 대비
- 신규 메뉴: `긴급 정비 접수` — 승인된 admin/staff/viewer 모두 접근
- Firebase 예정 경로: `maintenanceRecords` (`auth != null` 읽기·쓰기 규칙 필요)
- 기능: 정비 접수 등록·수정·삭제, 상태관리, 검색/필터, 금일·진행 통계
- 접수 필드: 차량번호, 차종, 의뢰자, 전화번호, 접수시간, 주행거리, 상태, 담당자, 요청사항, 내부메모
- 인쇄: 첨부 사진을 기준으로 한 A4 세로 정비 접수증(요청사항 줄 양식)
- 변경 파일: `un/index.html`, `un/styles.css`, `un/app-module.js`, `un/app-pages.js`, `un/app-maintenance.js`, `un/sw.js`
- 캐시 버전: `v125-2026-07-30`
- 롤백 태그: `rollback-before-emergency-maintenance-20260730`
- Firebase 규칙 백업: `firebase-rules-backups/realtime-before-maintenance-20260730.json`
- Firebase 규칙 게시 완료: `maintenanceRecords` 로그인 사용자 읽기·쓰기 허용
- 기능 커밋: `4288fbd` — 긴급 정비 접수 현황 및 인쇄 추가
- 규칙·백업 기록 커밋: `c969261`
- 운영 배포 및 라이브 확인 완료: 캐시 `v125`, 메뉴·현황·접수 모달·Firebase 연결 확인
- 기존 예약 현황판·견적도우미 유지 확인

## 2026-07-30 작업 기록 — 정비 접수증 A4 인쇄 수정

- 문제: 요청사항 영역 높이로 2장 출력, 배경 그래픽 미출력 시 밑줄 누락
- 수정: 전체 양식을 A4 1장 높이로 고정하고 요청사항을 실제 테두리 10칸으로 변경
- 변경 파일: `un/app-maintenance.js`, `un/index.html`, `un/sw.js`, `CODEX.md`
- 캐시 버전: `v126-2026-07-30`
- 롤백 태그: `rollback-before-maintenance-print-fix-20260730`
- 배포 커밋: `cc79d69` (`정비 접수증 A4 한 장 인쇄 수정`)
- 라이브 확인 완료: 요청사항 실제 테두리 10칸, 배경 그라데이션 제거, A4 고정 높이와 캐시 `v126` 반영 확인

## 2026-07-30 작업 기록 — 긴급 정비 접수 기능 종료

- 배경: BOLT 전산 정상화로 비상용 정비 접수 기능 운영 종료
- 제거: 긴급 정비 접수 메뉴, 현황 화면, 등록·수정·삭제·인쇄 코드, 전용 스타일과 캐시 항목
- 보존: 기존 `maintenanceRecords` 데이터는 복구 필요 가능성에 대비해 삭제하지 않음
- 변경 파일: `un/index.html`, `un/styles.css`, `un/app-module.js`, `un/app-pages.js`, `un/app-maintenance.js`(삭제), `un/sw.js`
- 캐시 버전: `v127-2026-07-30`
- 롤백 태그: `rollback-before-maintenance-removal-20260730`
- 배포 커밋: `886888e` (`긴급 정비 접수 기능 종료`)
- 라이브 확인 완료: 긴급 메뉴·현황·스크립트 제거, 캐시 `v127` 반영, 예약 현황판·견적도우미 유지 확인


## 2026-07-30 작업 기록 — 긴급 정비 접수 재활성화

- 배경: BOLT 전산 재장애로 비상용 정비 접수 기능 즉시 재활성화
- 복원: 긴급 정비 메뉴, 현황판, 등록·수정·삭제, A4 한 장 접수증 인쇄
- 데이터: 보존해 둔 `maintenanceRecords` 및 기존 Firebase 인증 규칙 그대로 사용
- 캐시 버전: `v128-2026-07-30`
- 재활성화 전 롤백 태그: `rollback-before-maintenance-reactivation-20260730`
- 배포 커밋: `d42cea2` (`긴급 정비 접수 재활성화`)
- 라이브 확인 완료: 긴급 메뉴·현황판·데이터 경로·A4 접수증 10칸·캐시 `v128` 반영 확인

## 2026-07-30 작업 기록 — 긴급 정비 빠른 현황 버튼

- 요청: 각 차량의 인쇄·수정 버튼 옆에서 현황을 빠르게 변경
- 추가: `현황 / 입고 / 출고 / 수리중` 버튼, 현재 상태 강조 표시
- 저장: 클릭 즉시 `maintenanceRecords/{id}/status` 갱신
- 상태 연결: 입고=`received`, 출고=`released`, 수리중=`working`
- 캐시 버전: `v129-2026-07-30`
- 롤백 태그: `rollback-before-maintenance-quick-status-20260730`
- 배포 커밋: `8bf3709` (`긴급 정비 빠른 현황 버튼 추가`)
- 라이브 확인 완료: 입고·출고·수리중 상태 저장 함수와 버튼 스타일, 캐시 `v129` 반영 및 기존 인쇄 10칸 유지 확인

## 2026-08-13 작업 기록 — 윈도우 PWA 설치 버튼 복구

- 문제: 대시보드 활성화 시 `.header-right`가 숨겨지면서 그 안의 설치 버튼도 보이지 않음
- 수정: 설치 버튼을 헤더 숨김 영역 밖으로 이동하고 우측 하단 고정 버튼으로 표시
- 동작: 설치 이벤트 준비 시 브라우저 설치창 실행, 미준비 시 Chrome/Edge 수동 설치 방법 안내
- 설치 완료 또는 standalone 실행 시 버튼 숨김
- 캐시 버전: `v130-2026-08-13`
- 롤백 태그: `rollback-before-windows-pwa-install-button-20260813`
- 배포 커밋: `42db501` (`윈도우 앱 설치 버튼 복구`)
- 라이브 확인 완료: 우측 하단 앱 설치 버튼, 설치 안내 fallback, standalone 숨김, 캐시 `v130` 반영 확인

## 2026-08-25 작업 기록 — 작업코드 메인·복수 큰 항목 선택

- 견적도우미 기본 화면을 작업코드 참고로 변경하고 기존 자동차 모형 견적은 두 번째 탭으로 보존
- 큰 항목 카드를 복수 선택한 뒤 선택된 항목의 코드만 한 팝업에서 통합 표시
- 동일 작업명·코드는 자동 중복 제거, 선택 전체 해제와 선택 개수 표시 제공
- 큰 항목 설정: 이름 수정, 순서 이동, 숨김·다시 표시, 원본 설정 복구
- 코드 기능: 클릭 복사, 즐겨찾기, 메모, 숨김, 선택 결과 내 검색
- 설정 저장: 기존 `workCodes` 권한을 상속하는 `workCodes/uiConfig`에 동기화하고 로컬 백업 병행
- 기존 정적 원본 `work-codes-data.js`는 수정하지 않음
- 변경 파일: `un/app-workcodes-ui.js`(신규), `un/index.html`, `un/styles.css`, `un/sw.js`, `CODEX.md`
- 캐시 버전: `v131-2026-08-25`
- 롤백 태그: `rollback-before-workcode-main-multiselect-20260825`
- 기능 배포 커밋: `7fba57b` (`작업코드 메인과 복수 항목 선택 추가`)
- 저장 키 보강 커밋: `89d657f` (슬래시 포함 코드의 Firebase 설정 저장 안전성 보강)
- 라이브 확인 완료: 큰 항목 15개 생성, 작업코드·차량 모형 2개 탭, 복수 선택·통합 팝업·설정 기능 및 캐시 `v131` 반영 확인

## 2026-08-25 작업 기록 — 작업코드표 중간 탭·코드 팝업 시인성 개선

- 견적도우미 상단 탭을 `작업코드 참고 / 작업코드표 / 차량 모형 견적` 3개로 변경
- 가운데 작업코드표 탭에 기존 검색창과 큰 카드형 최종본 표를 그대로 이동
- 차량 모형 화면의 기존 작업코드 참고 버튼도 가운데 작업코드표 탭으로 연결
- 선택 코드 팝업의 작업내용 글자를 18px 굵게, 코드번호를 20px 독립 강조 박스로 확대
- 검색창과 행 간격·테두리·모바일 배치를 함께 개선
- 캐시 버전: `v132-2026-08-25`
- 롤백 태그: `rollback-before-workcode-table-tab-visibility-20260825`
- 배포 커밋: `f5a236a` (`작업코드표 탭과 코드 시인성 개선`)
- 라이브 확인 완료: 3개 탭, 가운데 기존 작업코드표·검색창 이동, 작업내용 18px·코드번호 20px 및 캐시 `v132` 반영 확인

## 2026-08-25 작업 기록 — 작업코드 큰 항목·세부내용 편집

- 선택 코드 팝업에 작업내용별 위·아래 순서 변경 버튼 추가, 변경 순서 Firebase 동기화
- 큰 항목 설정에 새 큰 항목 추가, 이름 수정, 순서 변경, 숨김, 직접 추가한 표 삭제 제공
- 각 큰 항목에 작업내용과 코드 직접 추가 가능
- 직접 추가한 세부내용은 선택 코드 팝업에서 수정·삭제 가능
- 기존 정적 원본 작업코드와 3개 탭·현재 권한은 그대로 보존
- 설정 저장: `workCodes/uiConfig`의 `customCategories`, `extraItems`, `resultOrder`
- 캐시 버전: `v133-2026-08-25`
- 롤백 태그: `rollback-before-workcode-category-detail-editor-20260825`
- 배포 커밋: `ca6353c` (`작업코드 항목과 세부내용 편집 추가`)
- 라이브 확인 완료: 3개 탭·기존 15개 항목 유지, 새 큰 항목 추가·세부 작업 관리·순서 변경 UI 및 캐시 `v133` 반영 확인

## 2026-08-25 작업 기록 — 선택 코드 즐겨찾기 전체 상단 고정

- 여러 큰 항목을 함께 선택해도 별표를 누른 작업코드는 항목 구분 없이 선택 결과 최상단에 통합 표시
- 별표가 없는 작업코드는 즐겨찾기 그룹 아래에 표시
- 별표를 추가하거나 해제하면 팝업 목록을 즉시 다시 정렬
- 별표 그룹과 일반 그룹 각각에서 기존 위·아래 사용자 순서 유지
- 위·아래 이동으로 즐겨찾기 경계를 넘으려 하면 상단 고정 안내 표시
- 캐시 버전: `v134-2026-08-25`
- 롤백 태그: `rollback-before-workcode-favorites-first-20260825`
- 배포 커밋: `effd8b7` (`작업코드 즐겨찾기 상단 고정`)
- 라이브 확인 완료: 즐겨찾기 우선 정렬·그룹 경계 보호·캐시 `v134` 운영 반영 확인

## 2026-08-25 작업 기록 — 현황 목록 모바일 잘림 개선

- 문제: 휴대폰 현황 목록에 데스크톱 표의 `min-width: 980px`가 적용되어 상태 이후 열이 화면 오른쪽으로 잘림
- 수정: 700px 이하에서 차량 한 대를 화면 폭 100%의 2열 카드로 표시
- 표시: 차량번호·차종·연락처·위치·상태·보험·수리내용·렌트카·입고일·출고예정·관리까지 12개 열 전부 카드 안에 유지
- 위치·상태 버튼은 카드 칸 너비에 맞춰 자동 축소, 수리내용과 수정 버튼은 카드 전체 너비 사용
- 데스크톱 표 화면은 기존 방식 그대로 유지
- 캐시 버전: `v135-2026-08-25`
- 롤백 태그: `rollback-before-status-mobile-layout-20260825`
- 배포 커밋: `23d5840` (`현황 목록 모바일 잘림 개선`)
- 라이브 확인 완료: 새 CSS·모바일 카드 규칙·서비스워커 캐시 `v135` 운영 반영 확인
